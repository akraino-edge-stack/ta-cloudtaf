apiVersion: danm.k8s.io/v1 
kind: DanmNet
metadata:
  name: test-net1
  namespace: default
spec:
  NetworkID: test-net1
  Options:
    container_prefix: eth0
    host_device: ens3 
    rt_tables: 201
